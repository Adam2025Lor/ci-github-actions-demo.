name: CI Hello World

on:
  push:
    branches:
      - main

jobs:
  hello-ci:
    runs-on: self-hosted

    steps:
      - name: Check out code
        uses: actions/checkout@v3

      - name: Print Hello Message
        run: echo "Hello, CI with GitHub Actions!!"
